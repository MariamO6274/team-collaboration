import React from "react";

const ProductsCards = () => {
  return <>hello</>;
};

export default ProductsCards;



/**
 * I should have 20 cards, for that I need to create render list of 20 cards,
 * and each card should be passed with props, prop of Title and image
 * Inside my <Card/> I will render title along with some border
 */